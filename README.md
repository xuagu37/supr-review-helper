Install Ollama

Open a browser and go to https://ollama.com/download

Download the macOS installer and install it (drag to Applications if prompted).

Launch Ollama once (you should see a small Ollama icon in the menu bar). This starts the local server.


launchctl setenv OLLAMA_ORIGINS "chrome-extension://*"